'use strict';
const settings = {
    elements: {},
    posX: 0,
    posY: 0,
    player: false,
    enemies: [],
}

export default settings;
export let elements = settings.elements;
export let x = settings.posX;
export let y = settings.posY;